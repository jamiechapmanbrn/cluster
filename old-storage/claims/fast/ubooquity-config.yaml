kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ubooquity-config
spec:
  storageClassName:  fast-nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
