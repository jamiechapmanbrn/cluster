kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jackett-config
spec:
  storageClassName:  rook-ssd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
